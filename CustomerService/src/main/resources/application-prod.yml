server:
  port: 9030
eureka:
  client:
    service-url:
      defaultZone: http://prod:9000/eureka/
spring:
  profiles:
    include: git
  cloud:
    config:
      uri: http://localhost:8888
      profile: prod